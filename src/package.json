{
  "name": "vite-plugin-partial-prebundle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup index.ts --format esm --dts --out-dir dist --watch",
    "build": "tsup index.ts --format esm --dts --out-dir dist --clean"
  },
  "dependencies": {
    "esbuild": "^0.19.0",
    "unplugin-vue": "^4.0.0",
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^7.2.0",
    "typescript": "^5.3.3"
  }
}
