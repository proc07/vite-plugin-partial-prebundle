{
  "name": "vite-plugin-partial-prebundle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup index.ts --format esm --dts --out-dir dist --sourcemap --watch",
    "build": "tsup index.ts --format esm --dts --out-dir dist --sourcemap --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "esbuild": "0.24.0",
    "unplugin-vue": "^5.2.1",
    "unplugin-vue-jsx": "^0.4.0",
    "vite": "^5.0.0",
    "fast-glob": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^1.6.0",
    "tsup": "^7.2.0",
    "typescript": "^5.3.3"
  }
}
